= form_for(@event) do |f|
  = f.label :date, "Date (mm/dd/yyyy)"
  = f.text_field :date
  
  = f.label :time
  = f.text_field :time

  = f.label :location
  = f.select :location, Project::LOCATIONS
  = f.submit
